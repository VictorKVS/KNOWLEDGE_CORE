name: Knowledge Quality Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate-knowledge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install validator dependencies
        run: python -m pip install --upgrade pip pyyaml
      - name: Validate knowledge records
        run: python tools/validate_knowledge.py
      - name: Validate protected repository surfaces
        run: python tools/validate_repository_structure_protection.py
      - name: Validate professional domain maturity and review contracts
        run: python tools/validate_professional_domains.py
      - name: Test Security atomic requirement gate
        run: python tools/test_security_requirement_validation.py
      - name: Validate Security atomic requirements
        run: python tools/validate_security_requirements.py
      - name: Validate historical Security Knowledge snapshots
        run: python tools/validate_security_snapshots.py
